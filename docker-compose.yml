services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cpa-sub2-conv
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
    restart: unless-stopped
